import { IsdepricatedPipe } from './isdepricated.pipe';

describe('IsdepricatedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsdepricatedPipe();
    expect(pipe).toBeTruthy();
  });
});
